import 'package:flutter/material.dart';

/// All palette values for the "Digital Ink" design system.
abstract final class AppColors {
  // Backgrounds
  static const lightBackground = Color(0xFFF5F0E8);
  static const darkBackground = Color(0xFF0E0C0A);

  // Text
  static const lightText = Color(0xFF1A1714);
  static const darkText = Color(0xFFEDE8E0);

  // Accent — vermillion red, used sparingly
  static const accent = Color(0xFFC0392B);

  // Card surfaces
  static const cardLight = Color(0xFFFDFAF5);
  static const cardDark = Color(0xFF1A1714);

  // Brush stroke details
  static const brushStrokeLight = Color(0xFF2C2520);
  static const brushStrokeDark = Color(0xFFD4CFC8);
}
